version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 12
        - nvm use 12
        - yarn install
    build:
      commands:
        - yarn run prod
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
