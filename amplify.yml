version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 12
        - yarn install
    build:
      commands:
        - yarn run prod
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
